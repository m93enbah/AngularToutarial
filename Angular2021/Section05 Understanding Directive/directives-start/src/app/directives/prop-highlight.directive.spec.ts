import { PropHighlightDirective } from './prop-highlight.directive';

describe('PropHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PropHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
